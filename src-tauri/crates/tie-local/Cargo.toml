[package]
name = "tie-local"
version = "0.1.0"
description = "Tie local Markdown workspace storage (filesystem, no Tauri)"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tie-common = { path = "../tie-common" }
